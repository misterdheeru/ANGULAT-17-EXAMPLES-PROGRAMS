import { DropdowncustomPipe } from './dropdowncustom.pipe';

describe('DropdowncustomPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdowncustomPipe();
    expect(pipe).toBeTruthy();
  });
});
